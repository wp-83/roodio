<div>
    <!-- We must ship. - Taylor Otwell -->
</div>


@props([
  'type' => 'button',
  'variant' => 'primary'
])

{{-- <button
  type="{{ $type }}"
  {{ $attributes->merge([
    'class' => "px-4 py-2 rounded text-white bg-{$variant}"
  ]) }}
>
  {{ $slot }}
</button> --}}


{{-- <x-ui.button variant="secondary">
  Submit
</x-ui.button> --}}
