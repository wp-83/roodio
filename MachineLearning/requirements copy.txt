# --- Core Data Science (Windows Safe) ---
# PENTING: numpy harus di bawah 2.0.0 agar kompatibel dengan tensorflow/pytorch di Windows
numpy<2.0.0
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.13.0

# --- Audio Processing (Librosa Logic) ---
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0  # Backend untuk mp3 decoding di Windows

# --- Deep Learning (YAMNet & TensorFlow) ---
# Engine utama. Di Windows Native berjalan di CPU.
tensorflow>=2.15.0
# WAJIB: Library untuk load model YAMNet dari Google
tensorflow-hub>=0.16.1
# Utilities untuk progress bar
tqdm>=4.66.0
# Library gambar (dependensi internal Keras/Matplotlib)
Pillow>=10.0.0

# --- MLOps & Tracking (Evaluasi Skripsi) ---
# Untuk mencatat akurasi, confusion matrix, dan loss
mlflow>=2.10.0

# --- System & Deployment Tools ---
joblib>=1.3.0     # Untuk save/load LabelEncoder (.npy/.pkl)
flask>=3.0.0      # Untuk API server (app.py)

# --- Visualization (Untuk Laporan/Analisa) ---
matplotlib>=3.8.0
seaborn>=0.13.0

# ==========================================
# --- LEGACY / OPSIONAL (Boleh Dihapus) ---
# ==========================================
# Library di bawah ini TIDAK DIPAKAI di pipeline YAMNet.
# Hanya install jika Anda masih ingin menjalankan kode eksperimen lama.

# xgboost>=2.0.0
# torch>=2.2.0
# transformers>=4.38.0
# datasets>=2.17.0
# accelerate>=0.27.0
# pyarrow>=15.0.0
# wordcloud>=1.9.0